<Window x:Class="GradePilotInstaller.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cancel Installation"
        Width="400"
        Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        Background="{DynamicResource BrushBackground}"
        FontFamily="{StaticResource FontFamilyPrimary}"
        WindowStyle="SingleBorderToolWindow"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.xaml" />
                <ResourceDictionary Source="/Themes/Typography.xaml" />
                <ResourceDictionary Source="/Themes/Buttons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Padding="32">
        <StackPanel>
            <!-- Icon and Message -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="?" 
                           FontSize="32" 
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource BrushPrimaryAction}"
                           Margin="0,0,16,0"
                           VerticalAlignment="Top" />
                
                <TextBlock Grid.Column="1" 
                           Text="{Binding Message}" 
                           Style="{StaticResource TypographyBody}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center" />
            </Grid>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="No" 
                        Command="{Binding NoCommand}" 
                        Style="{StaticResource StyleSecondaryButton}" 
                        Margin="0,0,12,0"
                        MinWidth="80" />
                <Button Content="Yes" 
                        Command="{Binding YesCommand}" 
                        Style="{StaticResource StylePrimaryButton}" 
                        MinWidth="80" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
