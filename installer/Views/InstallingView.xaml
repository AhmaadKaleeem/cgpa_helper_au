<UserControl x:Class="GradePilotInstaller.Views.InstallingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="48,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Installing GradePilot" Style="{StaticResource TypographyH1}" />
        <TextBlock Grid.Row="1" Text="Please wait while we prepare your files." Style="{StaticResource TypographySubheading}" />

        <Border Grid.Row="2" Background="{DynamicResource BrushWhite}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" CornerRadius="4" Padding="24" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource TypographyBody}" Margin="0,0,0,16" />
                <ProgressBar Value="{Binding ProgressValue}" IsIndeterminate="{Binding IsIndeterminate}" Maximum="100" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
