<UserControl x:Class="GradePilotInstaller.Views.InstallingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Installing GradePilot" Style="{StaticResource TypographyH1}" />
        <TextBlock Grid.Row="1" Text="Please wait while we prepare your files." Style="{StaticResource TypographySubheading}" />

        <Border Grid.Row="2" Background="{DynamicResource BrushSurface}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" CornerRadius="4" Padding="24" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="{Binding CurrentStage}" Style="{StaticResource TypographyBody}" FontWeight="SemiBold" Margin="0,0,0,8" />
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource TypographyBody}" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,16" TextWrapping="Wrap" />
                
                <ProgressBar Value="{Binding ProgressValue}" IsIndeterminate="{Binding IsIndeterminate}" Maximum="100" Height="8" Margin="0,0,0,8" />
                
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding FilesProcessedText}" Style="{StaticResource TypographyCaption}" />
                    <TextBlock Grid.Column="1" Text="{Binding ProgressPercentageText}" Style="{StaticResource TypographyCaption}" FontWeight="Medium" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Error Banner (visible only on failure) -->
        <Border Grid.Row="3"
                Background="#FEE2E2"
                BorderBrush="#FCA5A5"
                BorderThickness="1"
                CornerRadius="4"
                Padding="16,12"
                Margin="0,12,0,0"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="Installation Failed" FontWeight="SemiBold" Foreground="#B91C1C" Margin="0,0,0,4" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#7F1D1D" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
