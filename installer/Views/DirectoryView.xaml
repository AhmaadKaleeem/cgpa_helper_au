<UserControl x:Class="GradePilotInstaller.Views.DirectoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="48,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Installation Folder" Style="{StaticResource TypographyH1}" />
        <TextBlock Grid.Row="1" Text="Choose where to save the GradePilot files on your computer." Style="{StaticResource TypographySubheading}" />

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding InstallPath, Mode=OneWay}" 
                     IsReadOnly="True"
                     VerticalAlignment="Center" 
                     Padding="8" 
                     BorderBrush="{DynamicResource BrushBorder}" 
                     BorderThickness="1" 
                     FontFamily="{StaticResource FontFamilyPrimary}" 
                     FontSize="14" />
                     
            <Button Grid.Column="2" Content="Browse" Style="{StaticResource StyleSecondaryButton}" MinWidth="80" />
        </Grid>

        <TextBlock Grid.Row="3" Text="Space required 15 MB" Style="{StaticResource TypographyCaption}" Margin="0,0,0,24" />
    </Grid>
</UserControl>
